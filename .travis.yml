language: php
php:
  - '7.0'
  - '7.1'
  - nightly
install:
  - composer install
services:
  - postgresql
before_script:
  - psql -c 'create database travis_ci_test;' -U postgres
addons:
  postgresql: "9.5"
