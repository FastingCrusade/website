language: php
php:
  - '7.0'
  - '7.1'
services:
  - postgresql
before_install:
  - "psql -c 'create database travis_ci_test;' -U postgres"
  - "touch .env"
install:
  - "composer install"
  - "./artisan key:generate"
  - "./artisan config:cache"
  - "./artisan migrate"
